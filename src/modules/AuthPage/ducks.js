export const moduleName = 'auth';

// название полей в сторе
const fieldInfo = 'info';

export const GET_INFO = `${moduleName}/GET_INFO`;

const initState = {};

export const authReducer = (state = initState, action) => {
  switch (action.type) {
      default:
          return state;
  }
};
